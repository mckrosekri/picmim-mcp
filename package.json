{
  "name": "picmim-mcp-public",
  "version": "1.2.0",
  "private": true,
  "description": "Grok Build plugin package and public readiness checks for Picmim's hosted MCP connector",
  "type": "module",
  "scripts": {
    "check": "node scripts/check-public-connector.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
